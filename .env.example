# The phone and development computer must be on the same network.
# Replace the IP with the computer's current LAN IP when it changes.
EXPO_PUBLIC_API_URL=http://10.215.160.133:8000/api/v1

# Day 6 AI package selector. Keep disabled until a server-side API key is set.
# Deterministic package generation and fallback work without OpenAI access.
LLM_SELECTION_ENABLED=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra
OPENAI_REASONING_EFFORT=low
OPENAI_TIMEOUT_SECONDS=6
OPENAI_MAX_CONCURRENCY=8
